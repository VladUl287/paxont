TODO:
    Split metadata to multiple types with specific fields
    JsonOptions replace converters from map to code gen func with switch if converters count is low
    If options presented use number id as cache key
    Optimize number converter
    Use custom utf-8 encoder with polyfill for multiplatform optimal work
    Optimize property equality speed
    Use code gen func with switch for cache if size is low
